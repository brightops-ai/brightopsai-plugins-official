{
  "description": "Global auto-capture: fires every N user messages and instructs the active Claude session to write a synthesis nugget to the wiki inbox. Mirrors the MemPalace hook pattern — zero LLM work in the hook itself, the active session does the writeback.",
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/wiki_capture_hook.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
