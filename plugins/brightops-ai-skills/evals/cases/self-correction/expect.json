{
  "description": "Later statement supersedes the earlier one. The brief must not frame the superseded choice as an alternative that was weighed and rejected. Naming it incidentally (a technical comparison) is allowed, and the Check block naming what was dropped is wanted, so the bare word is not itself a failure.",
  "questions": "none",
  "must_contain": ["postgres"],
  "must_not_contain": ["instead of redis", "rather than redis", "not redis", "redis was", "originally redis", "considered redis"],
  "assumptions": "optional"
}
